#include <bits/stdc++.h>

#define int long long
#define endl '\n'
using namespace std;

signed main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int N, M, Va, Vb;
  cin >> N >> M >> Va >> Vb;

  string dependencies[N];
  for (int i = 0; i < N; i++) {
    cin >> dependencies[i];
  }
  


  // list of A (we need to fulfill all of these)

  return 0;
}