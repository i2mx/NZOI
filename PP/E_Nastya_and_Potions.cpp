#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    for (size_t i = 0; i < t; i++)
    {
        int N, K;
        cin >> N >> K;

        int potions[N];
        
        for (size_t i = 0; i < N; i++)
            cin >> potions[i];

        int unlimited[K];

        for(size_t i = 0; i < K; i++)
            cin >> unlimited[K];

    }

    return 0;
}