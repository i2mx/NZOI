#include <bits/stdc++.h>

#define int long long
#define endl '\n'
using namespace std;

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // 1≤N≤10000
    int H,W,N;
    cin >> H >> W >> N;

    for (size_t i = 0; i < N; i++)
    {
        int x = 0;
        int y = 0;
        
    }
    



    return 0;
}

// we can assume horizontal segments only interesstc with vertical segments
// and that vertical segments only with horizontal ones